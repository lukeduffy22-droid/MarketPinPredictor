Start building only after these adjustments are in place. Then follow the build order.

Required adjustments before build

Per-symbol buffers: replace single INDEX_RING/FLOW_RING with INDEX_RINGS[SPX|NDX|DJI|RUT] and FLOW_RINGS[...].

Ingest aggregation: aggregate all index prints to 1-second bars; drop sub-second bursts.

Payload normalization: one schema at ingest only (IndexTick, OptTrade). No downstream parsing.

Cadence enforcement: hard-limit recompute frequency by τ bucket; return 429 if polled faster.

Freshness checks: return 503 if latest index tick is older than 5 s during RTH.

Close time accuracy: load US market holiday/early-close calendar; compute true close time per session.

OI cache boundary: load OI at 09:35 ET and 13:00 ET into a cache; endpoints never call REST.

Coefficients and RMSE: load per-symbol β_vwap, β_gamma, β_flow and τ-bucket RMSE from DB at startup; fail fast if missing.

Secrets and limits: pull Polygon key from env, set WS backoff with jitter, bound rings to 5,400 entries.

Streamlit separation: UI consumes FastAPI only; no shared state via st.session_state.

Build order

Implement per-symbol rings and normalized ingest with 1-second aggregation.

Wire /healthz to expose per-symbol freshness and ring lengths.

Implement time_et with holiday/early-close map and minutes_to_close_et.

Add OI cache service and inject get_oi(symbol) into levels and predict.

Load coefficients and RMSE buckets from DB at app start; abort if absent.

Implement /levels/eod?symbol=... using the symbol’s ring and OI cache.

Implement /predict/close with τ-weights, cadence guard, freshness guard, per-symbol betas/RMSE.

Add performance guards (@timed, heap snapshots) around predict path.

Run backtest job to populate RMSE buckets and validate the 15% MAE uplift target.

Connect Streamlit to the API; remove legacy in-process predictors.

Smoke tests to run before live

Health: each symbol shows index_seconds≈>3000 within RTH; status=ok.

Freshness: stop ingest; predict returns 503 within 5 s.

Cadence: hit predict every 100 ms after τ<15; receive 429s as expected.

Determinism: replay a recorded 15:45–16:00; identical predictions across runs.

Accuracy gate: backtest shows MAE_15_0 ≤ 0.85× MAE_VWAP_15_0 per symbol.

Runtime monitors

p99 predict latency ≤200 ms from 15:45–16:00 ET.

ΔRSS <100 MB over session; log heap every 5 minutes.

WS reconnect with exponential backoff and jitter; drop messages on backlog >2 seconds.

If the list above matches your repo state, build. If any item is missing, fix it first.