Objective

Identify why SPX gamma is valid while NDX and RUT gamma collapse to zero, without weakening validation or changing core math.

Constraints (Do Not Violate)

Do not modify compute_gex, sign conventions, or validation thresholds.

Do not relax sanity gates.

Do not start generic market collection without symbol-level comparison.

Do not introduce fallback or simulated data.

Phase 1 — Symbol-Level Pipeline Comparison (MANDATORY)

For SPX, NDX, and RUT, log and compare side-by-side:

Option root(s) used

Example: SPX → SPXW, NDX → ?, RUT → ?

Log the exact root strings passed to Polygon.

Contracts ingested

Total contracts fetched

Calls vs puts count

Count of contracts with non-zero OI

Strike coverage

Min strike, max strike

Number of unique strikes

Distance of strikes from spot (ATM coverage)

Post-filter survival

Contracts remaining after:

Expiry filters

OI filters

Gamma > 0 filters

Number of strikes passed into compute_aggregate_gex()

Phase 2 — Aggregation & Validation Trace

For each symbol:

Aggregation inputs

Sum of call_gex_per_strike

Sum of put_gex_per_strike

Count of non-zero GEX strikes

Validation gate behavior

Explicitly explain why:

SPX passes GATE2_GEX_CONCENTRATION

NDX/RUT fail it

Confirm whether failure is due to:

Single-strike dominance

Sparse option universe

Incorrect root selection

Phase 3 — Root Cause Classification (REQUIRED OUTPUT)

Classify the failure into exactly one of the following (no guessing):

A. Incorrect option root (e.g. wrong weekly root)

B. Incomplete option universe returned by Polygon

C. Over-aggressive filtering removing valid strikes

D. Expiry mismatch (0DTE vs non-0DTE)

E. Legitimate market condition (true concentration)

Provide evidence for the classification.

Phase 4 — Minimal Fix Proposal

Only after root cause is identified:

Propose the smallest possible fix, limited to:

Option root correction or

Filter parameter adjustment or

Symbol-specific expiry selection

Do not change:

Core GEX math

Snapshot architecture

Validation logic

Success Criteria (Must Be Met)

SPX, NDX, RUT all produce non-zero gross_gex when market conditions justify it

Validation gates still block structurally invalid gamma

No new WebSocket, REST, or caching changes introduced